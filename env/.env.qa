BASE_URL=https://qa-awal.colanapps.in
ENV_API=dev

# $env:ENV = 'stage'; npx playwright test tests/UI/sales/demo.spec.ts   # PowerShell
# set ENV=stage && npx playwright test tests/UI/sales/demo.spec.ts      # cmd
# ENV=stage npx playwright test tests/UI/sales/demo.spec.ts             # git bash / CI
